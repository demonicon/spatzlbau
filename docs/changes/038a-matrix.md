# 038a – Matrix: Ist-Zustand aller Ansichten

Grundlage für Claude Design (Systempass). Reine Bestandsaufnahme, keine Bewertung. Screenshots dazu:
`design/snapshot/2026-09-26-inventar/` (nicht committet, siehe `038a-abweichungen.md`).

## Tabelle 1 – sechs Ansichten × zwölf Merkmale

| # | Merkmal | Personen | Phasen | Timeline | Finanzen | Entscheidungen | Anfragen |
|---|---|---|---|---|---|---|---|
| 1 | Kopfzeile | Ein `renderHeader()` für alle vier Screens (`app/ui/chrome.js:59`) – Titel „Aufgaben", Countdown „N Tage bis Einzug · Datum · Umzug" (`chrome.js:73`), Vorschau-Chip, dieselben vier Nav-Pillen rechts (Anfragen-Pille trägt ihr Ergebnis-Badge auf jedem Screen, nicht nur bei Anfragen), Avatar außen | wie Personen | wie Personen | Titel „Finanzen", sonst identisch | Titel „Entscheidungen", sonst identisch | Titel „Anfragen", sonst identisch |
| 2 | Sub-Navigation | Segment „Personen/Phasen/Timeline" (`dashboard.js:214`) unter der Suchleiste | dieselben drei Pillen | dieselben drei Pillen | keine – Filterpillen liegen erst bei „Alle Posten" weiter unten | Pillen „alle/offen" (`decisions-filter`) direkt unter der Kopfzeile | keine |
| 3 | Listenform | 3 Karten-Spalten (Ich/Gemeinsam/Partner:in), nach Monat gruppiert, `taskHTML()` (`app/ui/task.js:59`) | 5 Kanban-Spalten (eine je Phase), gleiche Zeile wie Personen | 1 Spalte, chronologisch, Zeitstrahl-Rail links, „Heute"-Trenner | Tabelle `fin-posts-table` (≥900) bzw. Karten `fin-posts` (<900) für „Alle Posten"; Aufschlüsselungs-Zeilen + Ausgleich-Karte oben | Tabelle `dt-table`: Datum/Von/Entscheidung/Am Zug/Aktion | Liste aus `article.af-row`-Karten, keine Tabelle |
| 4 | Panel ≥900 | rechtes Aside ~390px; ohne Auswahl: keine 3. Spalte (kein Platzhaltertext); „×" schließt | wie Personen | wie Personen | kein Akte-Aside – Ausgleich/Rahmendaten-Aside ist immer da, unabhängig von einer Auswahl; Posten-Bearbeiten öffnet stattdessen inline in der Tabelle | wie Personen (Akte der Aufgabe hinter der Entscheidung) | fest zweispaltig, Liste links ~320px, Detail rechts; „leer" gibt es nur ohne jede Anfrage |
| 5 | Handy <900 | Akte klappt **inline** in der Liste auf, „×" an derselben Stelle | wie Personen | wie Personen | Posten-Editor klappt ebenfalls **inline** auf (derselbe dunkle Balken wie ≥900) | eigener **Seitenwechsel**: Zeile → Akte als Seite, „‹ Entscheidungen" zurück | eigener **Seitenwechsel**: Zeile → Anfrage als Seite, „‹ Anfragen" zurück |
| 6 | Filter | Signal-Kacheln (`tile sig-tile`, `dashboard.js:171`) über der Liste | keine – Filter sind die Spalten selbst | Chips „alle/Du/Gemeinsam/Anna" (`tl-owner`) unter der Sub-Navigation | Aufschlüsselungs-Zeilen (`fin-break-row`, `finanzen.js:171` – volle Breite, Pfeil ›, zugleich Filter-Button `data-act="fin-filter"`) oben + eigene Pillen „alle/offen/bezahlt/Rückfluss" bei „Alle Posten" | Pillen „alle/offen" | keine |
| 7 | Primäraktion | keine außerhalb der Akte (dort „Bearbeiten"/„Einverstanden") | wie Personen | wie Personen | „Überweisung erfassen" (Ausgleich-Karte), einziger `btn-primary` außerhalb der Akte | „Einverstanden" in der angehefteten Entscheidungskarte | „An Claude senden" beim Anlegen |
| 8 | Anlegen (+) | kein „+" sichtbar (Aufgaben entstehen anderswo, außerhalb des Auftrags) | wie Personen | wie Personen | „+ Posten" (`btn-text row fin-add`) am Ende der Tabelle | kein „+" (entsteht aus Kommentaren) | „+ Anfrage" (`btn-secondary`) oben rechts |
| 9 | Editor-Hülle | weiße Karte im Panel/inline, Kopf mit Titel, „Bearbeiten" oben, „Fertig/Abbrechen" im Formular (`editHTML`, `app/ui/detail.js:372`) | wie Personen | wie Personen | dunkler Vollbreite-Balken direkt in der Tabelle: „Abbrechen" · Titel · „Fertig" (`costHTML`/`costNewHTML`, `app/ui/detail.js:657`) | wie Personen (Akte-Editor) | kein eigener Editor in den Screenshots (nur Kategorie-Pillen beim Anlegen) |
| 10 | Leerzustand | Kachelreihe leer: „Zwischen euch ist nichts offen." (`kpis-empty`, `dashboard.js:167`) | Spalte ohne Filter: „Alles erledigt." / „Alles erledigt – nächste Fälligkeit am …"; mit Filter: „Nichts in dieser Auswahl." (`emptyText()`, `dashboard.js:320`) | „Nichts offen in dieser Auswahl." | „Nichts offen." (Zahlungen) / „Keine Zeile in dieser Auswahl." (Posten) | „Keine offenen Entscheidungen." / „Keine Entscheidungen." | „Noch keine Anfrage. […]" / „Noch keine Angebote." |
| 11 | Stand/Chips | Owner-Chip `own` (S/A/gemeinsam), Überfällig rot als Text, fristkritische Termine (≤7 Tage) gelb hinterlegt (Screenshot `personen-1280-panel`: „28.09.", „30.09."), Teilschritt-/Kommentarzahl als Klartext | wie Personen | wie Personen | Stand als Punkte + Text (`●○○ geschätzt`), Zahler-Chip `own`, kein Signal-Chip | `sig-chip waitme` „wartet auf dich", Bestätigt-Kreise `dc-circle` | Chip „Ergebnis da" (`own C`), Status als 4-stufige Leiter (Entwurf/gesendet/Ergebnis/entschieden) |
| 12 | Datum/Zahl | Listenzeilen `DD.MM.` aus `dueShort()`/`fmtShort()` (`app/state.js:117,162`, genutzt in `app/ui/task.js:87`); der Kopfzeilen-Countdown nutzt separat `fmtDay()` (`state.js:124`, Wochentag + Datum, z. B. „Mo 28.12." bzw. mit Jahr, wenn es vom aktuellen abweicht) | wie Personen | Zeitstrahl-Tage `DD.MM.` aus einer eigenen, lokalen `fmtDay()` (`app/views/timeline.js:24`, verdeckt die gleichnamige aus `state.js`), plus Tages-Countdown „T-98" | „fällig"-Spalte ruft `fmtDay()` aus `state.js` direkt (`dueText`, `finanzen.js:449`) statt wie die Aufgaben-Listen `dueShort()`/`fmtShort()`, **plus** eine vierte, eigene Monatsformatierung `MMM JJJJ` in `app/costs.js:201` | wie Personen | wie Personen |

Beträge überall `1.234 €` mit Tausenderpunkt; `font-variant-numeric: tabular-nums` in Tabellen/Listen.

## Tabelle 2 – Komponenten-Vorkommen

| Komponente | Datei(en) · Funktion/Zeile | Befund |
|---|---|---|
| Kopfzeile | `app/ui/chrome.js:59` `renderHeader()` | eine Quelle, 4 Aufrufer (`anfragen.js:295`, `dashboard.js:547`/`623`, `finanzen.js:729`) |
| Nav-Pille | `app/ui/chrome.js:66` (Closure `nav()` in `renderHeader`) | eine Quelle |
| Segment (Personen/Phasen/Timeline) | `app/views/dashboard.js:214–221` (`VIEWS`, `currentView`) | eine Quelle |
| Tabelle | `app/views/anfragen.js:183`; `app/views/dashboard.js:512`; `app/views/finanzen.js:55,249,315,362,384,387,520` | **9 unabhängige `<table>`-Definitionen in 3 Dateien, davon 7 allein in `finanzen.js`** – keine gemeinsame Tabellen-Komponente |
| Karte (Aufgabe) | `app/ui/task.js:59` `taskHTML()` | eine Quelle für Personen/Phasen/Timeline |
| Leiter/Stand | `app/costs.js` (Ladder-Logik), `app/ui/detail.js` (`ladderHTML`), `app/views/dashboard.js`/`finanzen.js` (eigene Balken-Markups), `app/main.js` (`data-pct`-Fülllogik) | Berechnung zentral, Markup an mehreren Stellen separat gebaut |
| Chip | `app/ui/task.js` (`sig-chip`), `app/ui/chrome.js` (`pill-badge`), `app/ui/detail.js` (`own`, `tag`), `app/views/dashboard.js`, `app/views/anfragen.js` | 5 Dateien, kein gemeinsamer Chip-Helfer |
| Editor-Hülle | `app/ui/detail.js:372` `editHTML()` (Akte) **und** `app/ui/detail.js:657` `costNewHTML()`/`costHTML()` (Finanzen) | 2 optisch unterschiedliche Hüllen (weiße Karte vs. dunkler Balken) für denselben Zweck |
| Leerzustand | `anfragen.js:76,252`; `dashboard.js:167,209,355,510,636,700`; `finanzen.js:305,518`; `print.js:63`; `timeline.js:239`; `detail.js:424` (`<span>`, nicht `<p>`) | **13 eigene Leertext-Stellen in 6 Dateien, mit drei verschiedenen Klassennamen für denselben Zweck** (`empty`, `kpis-empty` in `dashboard.js:167`, `col-empty` in `dashboard.js:355`) – kein gemeinsamer Helfer. Nicht mitgezählt: die drei `<aside class="panel empty">`-Platzhalter (`anfragen.js:285`, `dashboard.js:427,552`), die zu Merkmal 4 (Panel) gehören |
| Filter-Pille | `dashboard.js:519` (`decisions-filter`), `dashboard.js:587` (`filter-clear`), `finanzen.js:171` (`fin-filter` – zugleich die einzige „Kachel" von Finanzen, siehe unten), `finanzen.js:503` (`post-filter`), `finanzen.js:509` (`fin-filter-clear`) | 5 eigenständige Button-Templates, keine gemeinsame Komponente |
| Kachel | `dashboard.js:171` (`tile sig-tile`, quadratisches Raster) | eine Quelle; `finanzen.js:171` (`fin-break-row`) sieht ähnlich aus, ist aber eine volle Zeile mit Pfeil › und zugleich Filter-Button – keine zweite echte Kachel |
