# 014 – Bugfix-Bündel 2.0.2

Stand: 23.09.2026 · Version 2.0.1 → 2.0.2 · Branch: `fix/014` von `preview` · Modell: Sonnet · Aufwand: S · **Status: umgesetzt** (Abweichungen: `014-abweichungen.md`)
Sieben Punkte aus 1.1, 016c, 018 und dem Release-Check 2.0. Keine Funktion neu, keine Design-Entscheidung. Reihenfolge nach Sichtbarkeit für Anna.

## Punkte

1. **„Seit du zuletzt da warst" ohne Person.** `task_changes.changed_by = null` (Seed, Inhaltspaket, Connector) wird als „Inhaltspaket" beschriftet – „Halteverbot: Frist 11.12. → 04.12., Inhaltspaket" statt leer. Gleiche Regel im Besuchsblock und in der Akte, falls dort Änderungen stehen.
2. **„wartet auf n ›" Tap-Ziel** (Timeline und Aufgabenzeilen): 17 px hoch direkt unter dem Titel. Mindestens 44 px Hit-Area über Padding, ohne die Zeilenhöhe von 56 px zu sprengen (Padding negativ/pseudo-element), Abstand zum Titel-Link 8 px.
3. **Teilschritt ↑/↓ in Akte Bearbeiten** 28×22 px nebeneinander: auf 44×44 Hit-Area, oder – besser – die Pfeile durch den Ziehen-Griff aus 017 ersetzen, falls der schon existiert; sonst Pfeile untereinander mit 8 px Abstand.
4. **Zwei Primär-Buttons** in Akte Bearbeiten (Fertig + „Hinzufügen" im Kopf) und Umzugstag-Blatt (Drucken + „Hinzufügen"): „Hinzufügen" wird Sekundär. Regel 020: ein Primär je Ansicht.
5. **`log_task_change()`** für `anon`/`authenticated` aufrufbar (Supabase-Linter). Migration `017_b014_revoke.sql`: `revoke execute on function public.log_task_change() from public, anon, authenticated;` – Trigger läuft als Owner weiter. Prüfen: Frist in der App ändern → Zeile in `task_changes`; Linter-Warnung weg.
6. **Sortierung nach Kalenderdatum** statt `offset_days` in Spalten und Kostenliste (1.1 Abw. 4): `dueInfo(t).sort` verwenden. Sichtbar seit Umzugstag = 02.01. (Phase 3 mischt Anker).
7. **Posten in „Als Nächstes zahlen" und Liste** öffnet das Formular an beiden Stellen (016b/016c): nur die angetippte Stelle öffnet, die andere bleibt Zeile.

Dazu, ohne Nummer: `docs/changes/preview-2.0-status.md` auf den Stand bringen (Migrationen 010–016 angewendet), CLAUDE.md zwei Zeilen (freigegeben): „Datumsversionen haben drei oder vier Teile und beginnen mit 20" · „Eigenen Testserver nur über seine PID beenden, nie prozessweit."

## Akzeptanzkriterien

- [x] 380 px: „wartet auf 2 ›" trifft bei Tipp 8 px unter dem Titel die Abhängigkeit, nicht die Akte
- [x] Bearbeiten: Teilschritt verschieben mit Daumen ohne Fehltipp (Hit-Area ≥ 44 px, grep auf Button-Maße)
- [x] Grep: je Ansicht genau ein `.btn-primary`
- [x] Migration im Dry-Run (Claude prüft per Connector), Linter-Warnung weg, Trigger schreibt weiter
- [x] Phase-3-Spalte mit Umzugstag 02.01.: `packen` (05.12.) vor `halteverbot` (05.12., Anker Umzugstag) nach Datum, nicht nach Offset
- [x] „Seit du zuletzt da warst" mit der bestehenden `halteverbot`-Zeile zeigt „Inhaltspaket"
- [x] Ein Test je Punkt, Regression grün, Bericht ≤ 10 Zeilen, Changelog 2.0.2 (release "2.0"): „Kleine Stellen, die beim Tippen gestört haben, sind behoben."
- [x] Merge nach preview, dann `--no-ff` nach main, Tag v2.0.2 – vor 18 Uhr.
