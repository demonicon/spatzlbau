# Abweichungen – Auftrag 015 (Freeze / Version 1.0)

- **Tag `v1.0`:** nicht gesetzt, nicht gepusht, nicht gemergt – laut expliziter Anweisung macht Sebastian das nach der Abnahme selbst. Der Auftrag listet den Tag als Akzeptanzkriterium; das Kriterium bleibt bis dahin offen.
- **Release-Zuordnung der bestehenden Changelog-Einträge:** Der Auftrag verweist auf „die Tabelle in Abschnitt 2" (0.1–0.7). Die Tabellentitel beschreiben die Aufträge je Release, decken sich aber nicht immer wörtlich mit dem tatsächlichen Titel des jeweils ältesten passenden Changelog-Eintrags (z. B. Zeile 0.4 „Sicherer und mit täglicher Sicherung" für Auftrag 008/008b, der als reine Backup-Härtung keinen eigenen sichtbaren Changelog-Eintrag hat). Zugeordnet wurde deshalb chronologisch fortlaufend: der älteste bestehende Eintrag (`2026.09.22`) erhielt `release: "0.1"`, jeder folgende Eintrag der Reihe nach `0.2` … `0.7` – das ist die einzige Zuordnung, die mit der Deploy-Reihenfolge (Versionsnummern steigen monoton) vereinbar ist. Die Tabellentitel selbst wurden nirgends ins Changelog übernommen, nur in `docs/changes/015-freeze.md` als Dokumentation belassen.
- **„Erledigte Entscheidungen" (Abschnitt 6 des Auftrags, Ziel `docs/history.md`):** Zusammen mit Setup-Reihenfolge, Smoke-Test-Historie und der Roadmap vom 13.09. verschoben wurden genau diese drei Abschnitte aus `BRIEFING.md`. Die Tabelle „Technische Entscheidungen (getroffen, nicht neu diskutieren)" in `BRIEFING.md` Abschnitt 2 ist **nicht** verschoben worden – sie wird laufend als Architektur-Referenz gebraucht (Stack, Hosting, Auth) und zählt inhaltlich zu „Konzept/Datenmodell", das laut Auftrag vorne bleiben soll. Eine eigene, separat benannte Liste „erledigter Entscheidungen" existierte in `BRIEFING.md`/`CLAUDE.md` nicht – falls etwas anderes gemeint war, bitte konkret benennen.
- **Footer-Tooltip:** zeigt Datum + Build nur für den neuesten (1.0-)Eintrag, da nur dieser ein `date`-Feld trägt (ältere Einträge tragen ihr Datum weiterhin im `version`-Feld). Kein Datenverlust, nur keine zusätzliche Redundanz.

Alle übrigen Punkte wie im Auftrag beschrieben umgesetzt, keine weiteren Abweichungen.
