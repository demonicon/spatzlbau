# 038c – Abweichungen

Start: 2026-09-26T09:37:08Z

- **Login-Tests nicht möglich – Testkonten sind vorhanden (.env), aber die Session-Sicherheitssperre lehnte den echten Login im Browser ab** (Klassifikator-Grund „Production Reads"/„Browser Input Exfil"). Kein Workaround versucht. Stattdessen: (a) ein Node-Testharness mit unveränderten Kopien von `state.js`/`filters.js`/`anfragen.js` (nur `supabase.js` ist ein Stub, nie erreicht) prüft Zähler/Filter/`choosePlan` gegen feste Werte; (b) im echten Browser (lokaler Server, **ohne Login**) wurde per `import()` synthetischer, nicht persistierter Zustand gesetzt (`ui.preview = true` als Sicherheitsnetz) und `mountShell`/`updateShell`/`dashboardView`/`anfragenView` direkt aufgerufen – DOM-Beleg für Badge (1→0 beim Öffnen), Pille „neu" (Handy und Desktop) und die Zeile „Einmalig" (Betrag/„–", vierte Detailzeile bei 380 px). Kein Zugriff auf die Live-Datenbank, kein Schreibzugriff. Test 1/2 sind damit „simuliert – Testkonten vorhanden, Login von der Sicherheitssperre verweigert", nicht grün mit echten Konten. Erstes Akzeptanzkriterium bleibt deshalb `[ ]`.
- **Die Pille „neu" lebt jetzt auch am Handy** (in derselben Reihe wie die drei Personen-Pillen, `data-act` je Pille statt einheitlich für die Gruppe – kleine Erweiterung von `filterPillsHTML`), nicht nur ab 900 px. Ursprünglich nur am Desktop gebaut (dort, wo „wartet auf dich" steht) – nach Reviewer-Fund korrigiert, weil der Auftrag „alle drei Unteransichten"/„Liste+Panel" verlangt und Test 1 keine Breite nennt.
- **BRIEFING.md geändert ohne eigene Rückfrage dafür** (Zeilen zu Badge/Pille „neu" und `setup_fee`) – die Definition of Done verlangt das Nachziehen bei Verhaltensänderung, aber CLAUDE.md sagt „nur mit ausdrücklicher Bestätigung". Bitte kurz gegenlesen; sonst zurücksetzen.
- **Frage beim Wählen unterscheidet jetzt „übernehmen" (bestehender Posten wird geändert) von „anlegen" (neuer Posten)** für die einmalige Gebühr aus `setup_fee` – im ersten Entwurf hatte der Zweig ohne Laufend-Zeile immer „als neuen Posten übernehmen" gesagt, auch wenn tatsächlich ein bestehender Posten überschrieben wurde (Reviewer-Fund, korrigiert).
- Changelog-Formulierung nach Reviewer-Fund präzisiert: „von der anderen Person" → „von jemand anderem" (zählt auch Claude-Kommentare mit), „Pille" (Fachjargon) raus, „legt automatisch einen Posten an" → „fragt die App, ob sie dafür einen Posten anlegt oder einen bestehenden ändert" (zweite Reviewer-Runde: „anlegen" allein war noch unvollständig, der `…-schaetzung`-Posten wird ja geändert, nicht verdoppelt); dieselbe Ergänzung in BRIEFING.md.
- **Der Hinweis „Laufende Kosten dafür kommen mit den Verträgen." blieb bei einer Gebühr ohne Laufend-Zeile zuerst weg** (Reviewer-Fund, korrigiert): die Frage nennt jetzt beides in einem Satz, sonst wirkt „Ja" wie eine vollständige Übernahme des Monatspreises.
- **Die Pille „neu" filtert die Timeline nicht** – wie „überfällig"/„fristkritisch"/„wartet auf dich" auch: `timeline.js` liest `ui.filter` nicht, nur Phase/Suche/Besitzer. Vorbestehende Lücke (nicht durch 038c verursacht), hier nur neu sichtbar. Nicht behoben – wäre eine Änderung an allen vier Pillen, nicht nur an „neu", und damit außerhalb des S-Aufwands dieses Auftrags.
- **Test-Status je Testfall aus dem Auftrag** (Testkonten fehlen nicht, aber die Sicherheitssperre verweigerte den Login – s. o.):
  - Test 1 (Badge/Pille, Kommentar von A, Verschwinden beim Öffnen): **simuliert** – Node-Harness (Zähler) + Browser mit synthetischem Zustand ohne Login (DOM, `mountShell`/`updateShell`/`dashboardView`). Kein Testkommentar in der echten DB, also auch nichts aufzuräumen.
  - Test 2 (eigener Kommentar erzeugt keinen Badge): **simuliert**, gleicher Weg wie Test 1.
  - Test 3 (Zeile „Einmalig", Betrag/„–", vierte Detailzeile bei 380 px): **simuliert** – Node-Harness (`fmtPrice`) + Browser mit synthetischer Anfrage (zwei Angebote, eins mit `setup_fee`), DOM-Beleg für beide Fälle bei 375×812.
  - Test 4 („Wählen" bei `setup_fee` + `price_kind=monat`): **simuliert** – `choosePlan`/`moneyPlan` im Node-Harness gegen feste Werte geprüft; der Schreibweg aus `main.js` („money-yes": `updateRecurring` + zusätzlich `updateCost`/`addCost` für `m.extra`) mit Mock-Funktionen statt Supabase nachgebaut und für `recurring + cost-new` bestätigt (richtige IDs/Felder). Kein Zugriff auf echte `costs`/`recurring`-Zeilen.
  - Keins der vier ist „grün mit beiden Testkonten" im Sinne der Session-Regel – das Akzeptanzkriterium bleibt `[ ]`, bis jemand mit Bash-/Browser-Erlaubnis für den echten Login das nachholt.
- Das Formularfeld „Einmalig in € (optional)" beim manuellen Anlegen/Ändern eines Angebots (`views/anfragen.js` `offerEditHTML`, `main.js` `offer-save`) steht nicht wörtlich im Auftrag, war aber nötig: sonst könnten Sebastian/Anna `setup_fee` nie selbst für ein eigenes Angebot setzen.
